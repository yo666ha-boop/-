name: Audit Future root candidate stability
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
  pull-requests: write
jobs:
  audit:
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Playwright WebKit
        run: |
          npm init -y
          npm install playwright@1.55.0
          npx playwright install --with-deps webkit
      - name: Start COOP COEP server
        run: |
          cat >/tmp/coi-root.mjs <<'NODE'
          import http from'http';import fs from'fs';import path from'path';const root=process.cwd(),mime={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.wasm':'application/wasm','.bin':'application/octet-stream','.jpg':'image/jpeg','.png':'image/png','.webp':'image/webp'};http.createServer((q,s)=>{let n=decodeURIComponent(new URL(q.url,'http://x').pathname);if(n.endsWith('/'))n+='index.html';const f=path.resolve(root,'.'+n);if(!f.startsWith(root)||!fs.existsSync(f)){s.writeHead(404);return s.end('404')}s.setHeader('Cross-Origin-Opener-Policy','same-origin');s.setHeader('Cross-Origin-Embedder-Policy','require-corp');s.setHeader('Cross-Origin-Resource-Policy','same-origin');s.setHeader('Cache-Control','no-store');s.setHeader('Content-Type',mime[path.extname(f).toLowerCase()]||'application/octet-stream');fs.createReadStream(f).pipe(s)}).listen(4199,'127.0.0.1');
          NODE
          node /tmp/coi-root.mjs >/tmp/coi-root.log 2>&1 &
          sleep 1
      - name: Run root candidate audit
        run: |
          set -o pipefail
          node .github/scripts/audit-future-root-candidates-20260819.mjs 2>&1 | tee /tmp/root.log
          test "$(grep -c '^ROOT_REF ' /tmp/root.log)" -eq 4
          test "$(grep -c '^ROOT_ROW ' /tmp/root.log)" -eq 32
          test "$(grep -c '^ROOT_SUMMARY ' /tmp/root.log)" -eq 1
      - name: Report
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          { echo '### Future root candidate stability audit'; echo '```text'; grep -E '^(ROOT_REF|ROOT_ROW|ROOT_SUMMARY)' /tmp/root.log || true; echo '```'; } >/tmp/root-report.md
          gh pr comment ${{ github.event.pull_request.number }} --body-file /tmp/root-report.md
