name: Verify tournament live real mate 21605

on:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  firefox-live-mate:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Syntax check live mate verifier
        run: node --check .github/scripts/verify-tournament-vercel-live21605.mjs
      - name: Install Playwright Firefox
        run: |
          npm init -y
          npm install playwright@1.55.0
          sudo rm -f /etc/apt/sources.list.d/google-chrome.list /etc/apt/sources.list.d/google-chrome.sources
          npx playwright install --with-deps firefox
      - name: Verify real mate result advances tournament
        env:
          PREVIEW_URL: https://mitsuki-shogi-fullapp-unified21602.vercel.app
        run: |
          set -o pipefail
          node .github/scripts/verify-tournament-vercel-live21605.mjs 2>&1 | tee /tmp/tournament-vercel-live21605.log
      - name: Upload live mate diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tournament-vercel-live21605
          if-no-files-found: warn
          path: /tmp/tournament-vercel-live21605.log
