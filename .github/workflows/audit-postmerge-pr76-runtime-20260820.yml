name: Audit postmerge PR76 runtime
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
jobs:
  local-runtime:
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    container:
      image: mcr.microsoft.com/playwright:v1.55.0-jammy
    strategy:
      fail-fast: false
      matrix:
        platform: [iphone, fire, desktop, firefox]
    steps:
      - uses: actions/checkout@v4
      - name: Prepare Playwright package
        run: |
          npm init -y
          npm install playwright@1.55.0
      - name: Start COOP COEP server
        run: |
          cat >/tmp/coi-postmerge76.mjs <<'NODE'
          import http from'http';import fs from'fs';import path from'path';const root=process.cwd(),mime={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css','.wasm':'application/wasm','.bin':'application/octet-stream','.jpg':'image/jpeg','.jpeg':'image/jpeg','.png':'image/png','.webp':'image/webp'};http.createServer((q,s)=>{let n=decodeURIComponent(new URL(q.url,'http://x').pathname);if(n.endsWith('/'))n+='index.html';const f=path.resolve(root,'.'+n);if(!f.startsWith(root)||!fs.existsSync(f)){s.writeHead(404);return s.end('404')}s.setHeader('Cross-Origin-Opener-Policy','same-origin');s.setHeader('Cross-Origin-Embedder-Policy','require-corp');s.setHeader('Cross-Origin-Resource-Policy','same-origin');s.setHeader('Cache-Control','no-store');s.setHeader('Content-Type',mime[path.extname(f).toLowerCase()]||'application/octet-stream');fs.createReadStream(f).pipe(s)}).listen(4239,'127.0.0.1');
          NODE
          node /tmp/coi-postmerge76.mjs >/tmp/coi-postmerge76.log 2>&1 &
          sleep 1
      - name: Run local postmerge regression
        shell: bash
        env:
          TARGET: local
          PLATFORM: ${{ matrix.platform }}
          HOME: /root
        run: |
          set -euo pipefail
          node .github/scripts/audit-postmerge-pr76-runtime-20260820.mjs 2>&1 | tee /tmp/post76.log
          grep -q '^PASS_POSTMERGE_PR76_RUNTIME ' /tmp/post76.log
      - if: always()
        run: grep -E '^(POSTMERGE_PR76_RUNTIME|PASS_POSTMERGE_PR76_RUNTIME|Error:)' /tmp/post76.log || true
  public-pages:
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    container:
      image: mcr.microsoft.com/playwright:v1.55.0-jammy
    strategy:
      fail-fast: false
      matrix:
        platform: [iphone, fire, desktop]
    steps:
      - uses: actions/checkout@v4
      - name: Prepare Playwright package
        run: |
          npm init -y
          npm install playwright@1.55.0
      - name: Run public Pages postmerge regression
        shell: bash
        env:
          TARGET: public
          PLATFORM: ${{ matrix.platform }}
          HOME: /root
        run: |
          set -euo pipefail
          for n in 1 2 3 4 5; do
            if node .github/scripts/audit-postmerge-pr76-runtime-20260820.mjs 2>&1 | tee /tmp/post76-public.log && grep -q '^PASS_POSTMERGE_PR76_RUNTIME ' /tmp/post76-public.log; then exit 0; fi
            echo "public Pages retry $n"; sleep 20
          done
          exit 1
      - if: always()
        run: grep -E '^(POSTMERGE_PR76_RUNTIME|PASS_POSTMERGE_PR76_RUNTIME|Error:)' /tmp/post76-public.log || true
