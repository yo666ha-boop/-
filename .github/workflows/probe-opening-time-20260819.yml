name: Future opening time budget probe
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
  pull-requests: write
jobs:
  opening:
    runs-on: ubuntu-22.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Prepare Playwright with runner Chrome
        run: |
          npm init -y
          npm install playwright@1.55.0
          google-chrome --version
      - name: Start COOP COEP static server
        run: |
          cat >/tmp/coi-open.mjs <<'NODE'
          import http from'http';import fs from'fs';import path from'path';const root=process.cwd(),mime={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css','.wasm':'application/wasm','.bin':'application/octet-stream','.jpg':'image/jpeg','.png':'image/png','.webp':'image/webp'};http.createServer((q,s)=>{let n=decodeURIComponent(new URL(q.url,'http://x').pathname);if(n.endsWith('/'))n+='index.html';const f=path.resolve(root,'.'+n);if(!f.startsWith(root)||!fs.existsSync(f)){s.writeHead(404);return s.end('404')}s.setHeader('Cross-Origin-Opener-Policy','same-origin');s.setHeader('Cross-Origin-Embedder-Policy','require-corp');s.setHeader('Cross-Origin-Resource-Policy','same-origin');s.setHeader('Cache-Control','no-store');s.setHeader('Content-Type',mime[path.extname(f).toLowerCase()]||'application/octet-stream');fs.createReadStream(f).pipe(s)}).listen(4189,'127.0.0.1');
          NODE
          node /tmp/coi-open.mjs >/tmp/open-server.log 2>&1 &
          sleep 1
      - name: Compare 3.0 3.5 and 4.5 second opening searches
        run: node .github/scripts/probe-opening-time-20260819.mjs 2>&1 | tee /tmp/opening-time.log
      - name: Report
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          { echo '### Opening time budget probe'; echo '```text'; grep -E '^(OPEN_PROFILE|OPEN_REF|OPEN_ROW|OPEN_SUMMARY)' /tmp/opening-time.log 2>/dev/null | tail -120 || true; echo '```'; } >/tmp/opening-report.md
          gh pr comment ${{ github.event.pull_request.number }} --body-file /tmp/opening-report.md
