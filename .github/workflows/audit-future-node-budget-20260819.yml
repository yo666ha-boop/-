name: Audit Future fixed-node middlegame budget
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
  pull-requests: write
jobs:
  audit:
    runs-on: ubuntu-22.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install Playwright WebKit
        run: |
          npm init -y
          npm install playwright@1.55.0
          npx playwright install --with-deps webkit
      - name: Start COOP COEP server
        run: |
          cat >/tmp/coi-node.mjs <<'NODE'
          import http from'http';import fs from'fs';import path from'path';const root=process.cwd(),mime={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.wasm':'application/wasm','.bin':'application/octet-stream','.jpg':'image/jpeg','.png':'image/png','.webp':'image/webp'};http.createServer((q,s)=>{let n=decodeURIComponent(new URL(q.url,'http://x').pathname);if(n.endsWith('/'))n+='index.html';const f=path.resolve(root,'.'+n);if(!f.startsWith(root)||!fs.existsSync(f)){s.writeHead(404);return s.end('404')}s.setHeader('Cross-Origin-Opener-Policy','same-origin');s.setHeader('Cross-Origin-Embedder-Policy','require-corp');s.setHeader('Cross-Origin-Resource-Policy','same-origin');s.setHeader('Cache-Control','no-store');s.setHeader('Content-Type',mime[path.extname(f).toLowerCase()]||'application/octet-stream');fs.createReadStream(f).pipe(s)}).listen(4198,'127.0.0.1');
          NODE
          node /tmp/coi-node.mjs >/tmp/coi-node.log 2>&1 &
          sleep 1
      - name: Run fixed-node audit
        run: |
          set -o pipefail
          node .github/scripts/audit-future-node-budget-20260819.mjs 2>&1 | tee /tmp/node.log
          test "$(grep -c '^NODE_REF ' /tmp/node.log)" -eq 4
          test "$(grep -c '^NODE_ROW ' /tmp/node.log)" -eq 32
      - name: Report
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          { echo '### Future fixed-node middlegame audit'; echo '```text'; grep -E '^(NODE_REF|NODE_ROW)' /tmp/node.log || true; echo '```'; } >/tmp/node-report.md
          gh pr comment ${{ github.event.pull_request.number }} --body-file /tmp/node-report.md
